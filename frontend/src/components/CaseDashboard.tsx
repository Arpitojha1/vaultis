import { useState } from 'react'; import { Activity, ArrowRight, FolderLock, Search } from 'lucide-react'; import type { ApiCase, User } from '../types';
export function CaseDashboard({ cases,user,onSelect,onAudit }: {cases:ApiCase[];user:User;onSelect:(item:ApiCase)=>void;onAudit:()=>void}) { const [query,setQuery]=useState(''); const visible=cases.filter(c=>(c.case_number+c.title).toLowerCase().includes(query.toLowerCase())); return <div className="mx-auto max-w-7xl p-6"><section className="flex flex-wrap justify-between gap-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Authenticated operator</p><h1 className="mt-1 text-xl font-bold text-slate-900">{user.username} <span className="ml-2 text-sm font-medium text-slate-600 bg-slate-100 px-2 py-0.5 rounded">{user.role.replaceAll('_',' ')}</span></h1></div><button onClick={onAudit} className="rounded-md border border-slate-300 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 transition-colors"><Activity className="mr-1.5 inline w-4" strokeWidth={1.5}/>Audit chain</button></section><div className="mt-8 flex flex-wrap items-end justify-between gap-4"><div><h2 className="text-2xl font-bold tracking-tight text-slate-900">Accessible case vaults</h2><p className="mt-1 text-sm text-slate-500">Data is scoped by your active server session.</p></div><label className="relative"><Search className="absolute left-3 top-2.5 w-4 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search cases" className="rounded-md border border-slate-300 py-2 pl-9 pr-3 text-sm focus:outline-none focus:ring-1 focus:ring-slate-900 focus:border-slate-900"/></label></div><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{visible.map(item=><button key={item.case_id} onClick={()=>onSelect(item)} className="group rounded-xl border border-slate-200 bg-white p-6 text-left hover:border-slate-400 hover:shadow-sm transition-all"><FolderLock className="text-slate-700 w-6 h-6" strokeWidth={1.5}/><p className="mt-4 font-mono text-xs font-semibold text-slate-500">{item.case_number}</p><h3 className="mt-1 text-lg font-semibold text-slate-900">{item.title}</h3><p className="mt-2 text-sm text-slate-600 capitalize">{item.status}</p><span className="mt-6 flex items-center text-sm font-medium text-slate-900 group-hover:text-slate-600 transition-colors">View documents <ArrowRight className="ml-1 w-4 h-4"/></span></button>)}</div>{!visible.length&&<p className="mt-12 text-center text-slate-500">No accessible cases match your search.</p>}</div> }
