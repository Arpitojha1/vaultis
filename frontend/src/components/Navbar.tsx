import { Activity, Layers, Lock, LogOut, Shield, FolderPlus } from 'lucide-react'; import type { ApiCase, User } from '../types';
export function Navbar({ user, screen, currentCase, onNavigate, onLogout }: { user: User; screen: string; currentCase: ApiCase | null; onNavigate: (screen: 'dashboard'|'chat'|'audit'|'prepare')=>void; onLogout:()=>void }) { const label = user.role.replaceAll('_',' '); return <header className="sticky top-0 z-20 border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4"><button onClick={()=>onNavigate('dashboard')} className="flex items-center gap-2 font-bold"><span className="rounded-lg bg-slate-900 p-2 text-white"><Shield className="w-5" strokeWidth={1.5}/></span>VAULTIS</button><nav className="flex gap-1"><button onClick={()=>onNavigate('prepare')} className={`rounded-md px-3 py-2 text-xs font-semibold transition-colors ${screen==='prepare'?'bg-slate-100 text-slate-900':'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}><FolderPlus className="mr-1.5 inline w-4" strokeWidth={1.5}/>Prepare Workspace</button><button onClick={()=>onNavigate('dashboard')} className={`rounded-md px-3 py-2 text-xs font-semibold transition-colors ${screen==='dashboard'?'bg-slate-100 text-slate-900':'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}><Layers className="mr-1.5 inline w-4" strokeWidth={1.5}/>Cases</button>{currentCase && <button onClick={()=>onNavigate('chat')} className={`rounded-md px-3 py-2 text-xs font-semibold transition-colors ${screen==='chat'?'bg-slate-100 text-slate-900':'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}><Lock className="mr-1.5 inline w-4" strokeWidth={1.5}/>Assistant</button>}<button onClick={()=>onNavigate('audit')} className={`rounded-md px-3 py-2 text-xs font-semibold transition-colors ${screen==='audit'?'bg-slate-100 text-slate-900':'text-slate-600 hover:bg-slate-50 hover:text-slate-900'}`}><Activity className="mr-1.5 inline w-4" strokeWidth={1.5}/>Audit</button></nav><div className="flex items-center gap-3"><span className="hidden text-right text-xs sm:block"><strong className="block text-slate-900">{user.username}</strong><span className="capitalize text-slate-500">{label}</span></span><button onClick={onLogout} title="Log out" className="rounded-md p-2 text-slate-500 hover:bg-slate-100 hover:text-slate-900 transition-colors"><LogOut className="w-4" strokeWidth={1.5}/></button></div></div></header> }
