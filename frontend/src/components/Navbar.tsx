import { Activity, Layers, Lock, LogOut, Shield, FolderPlus } from 'lucide-react'; import type { ApiCase, User } from '../types';
export function Navbar({ user, screen, currentCase, onNavigate, onLogout }: { user: User; screen: string; currentCase: ApiCase | null; onNavigate: (screen: 'dashboard'|'chat'|'audit'|'prepare')=>void; onLogout:()=>void }) { const label = user.role.replaceAll('_',' '); return <header className="sticky top-0 z-20 border-b bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4"><button onClick={()=>onNavigate('dashboard')} className="flex items-center gap-2 font-bold"><span className="rounded-lg bg-blue-700 p-2 text-white"><Shield className="w-5"/></span>VAULTIS</button><nav className="flex gap-1"><button onClick={()=>onNavigate('prepare')} className={`rounded px-3 py-2 text-xs font-semibold ${screen==='prepare'?'bg-blue-50 text-blue-700':''}`}><FolderPlus className="mr-1 inline w-4"/>Prepare Workspace</button><button onClick={()=>onNavigate('dashboard')} className={`rounded px-3 py-2 text-xs font-semibold ${screen==='dashboard'?'bg-blue-50 text-blue-700':''}`}><Layers className="mr-1 inline w-4"/>Cases</button>{currentCase && <button onClick={()=>onNavigate('chat')} className={`rounded px-3 py-2 text-xs font-semibold ${screen==='chat'?'bg-blue-50 text-blue-700':''}`}><Lock className="mr-1 inline w-4"/>Assistant</button>}<button onClick={()=>onNavigate('audit')} className={`rounded px-3 py-2 text-xs font-semibold ${screen==='audit'?'bg-blue-50 text-blue-700':''}`}><Activity className="mr-1 inline w-4"/>Audit</button></nav><div className="flex items-center gap-3"><span className="hidden text-right text-xs sm:block"><strong className="block">{user.username}</strong><span className="capitalize text-slate-500">{label}</span></span><button onClick={onLogout} title="Log out" className="rounded p-2 text-slate-500 hover:bg-red-50 hover:text-red-700"><LogOut className="w-4"/></button></div></div></header> }
